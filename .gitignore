# Sorties de build : l'APK se récupère depuis les releases, pas depuis le dépôt.
build/
*.apk
*.apk.idsig

# Matériel de signature : ne doit jamais être versionné.
# Chaque poste régénère le sien au premier build.sh
*.keystore
*.jks
